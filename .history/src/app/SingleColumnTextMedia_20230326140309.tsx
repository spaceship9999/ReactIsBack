interface button {
    link: string,
    title: string
}

export default function SingleColumnTextMedia(props: { heading: string, content: string, cta: button}) {
    
    return (
        <div className="py-12"></div>
    )
}
